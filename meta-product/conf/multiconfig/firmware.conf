# Configuration for building the TF-A Firmware Interface Package (FIP).
# It includes u-boot configured as EFI provider.
TMPDIR .= "-${BB_CURRENT_MC}"

PREFERRED_PROVIDER_virtual/bootloader = "u-boot"
